const styles = (theme) => {
  return {
    mainContainer: {},
    card: {
      padding: theme.spacing(1, 2),

      height: "100%",
    },
    cardText: {
      fontSize: "14px",
      fontWeight: "800",
      color: theme.palette.primary.sidebar,
    },
  };
};

export default styles;
